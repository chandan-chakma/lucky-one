import React from 'react';

const Faq = () => {
    return (
        <div>
            <h1>How does react work</h1>
            <p>As we know that in react we use html but its not real html its called jsx. react work one way binding.react dom maintains virtual dom for maintains.virtual dom is a soft copy.</p>

            <h1>props vs state</h1>
            <p>props are read only.props can not modified.Otherside state can change asynchronusly and state can be modified using this.setstate</p>

        </div>
    );
};

export default Faq;